//import {uploadOnCloudinary} from ""
//import {ApiError}
//import {Product}
//const imageLocalpath=req.files?.productImage[0]?.path
/*
    if(!imageLocalpath)
    {
        throw new ApiError(400,"Image is Required")
    }
    const productImage=await uploadOnCloudnary(imageLocalpath)

    if(!productImage)
    {
        throw new ApiError(400,"Image Required")
    }
    Product.create({
        description:
        name:
        image:productImage.url,
        price:,
        stock:,
        category:,
        owner:,
    })
*/